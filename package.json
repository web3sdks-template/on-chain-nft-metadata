{
  "name": "hardhat-javascript-starter",
  "scripts": {
    "build": "npx web3sdks@latest detect",
    "deploy": "npx web3sdks@latest deploy",
    "release": "npx web3sdks@latest release"
  },
  "devDependencies": {
    "hardhat": "^2.10.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.8.0",
    "@web3sdks/contracts": "^1.0.0"
  }
}
